// The profanity filter is quite strict so we need to remove some words.
// I'll rely on community feedback to adjust the below list.
// package used is - https://www.npmjs.com/package/bad-words
const acceptableWords = ['god', 'balls', 'damn', 'ass', 'butt', 'sadist']

export default acceptableWords
